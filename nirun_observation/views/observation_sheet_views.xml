<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2021 NSTDA
  -->
<odoo>
    <record id="ob_sheet_action" model="ir.actions.act_window">
        <field name="name">Observations</field>
        <field name="res_model">ni.observation.sheet</field>
        <field name="view_mode">tree,form,graph</field>
    </record>
    <record id="ob_sheet_action_form" model="ir.actions.act_window">
        <field name="name">Observations</field>
        <field name="res_model">ni.observation.sheet</field>
        <field name="view_mode">form</field>
        <field name="target">current</field>
    </record>
    <record id="ob_sheet_view_search" model="ir.ui.view">
        <field name="name">ob.sheet.view.search</field>
        <field name="model">ni.observation.sheet</field>
        <field name="arch" type="xml">
            <search>
                <field name="patient_id" />
                <field name="encounter_id" />
                <filter
                    name="effective_today"
                    string="Today"
                    domain="[
                    ('effective_date', '&gt;=', datetime.datetime.now().strftime('%Y-%m-%d 00:00:00')),
                    ('effective_date', '&lt;=',datetime.datetime.now().strftime('%Y-%m-%d 23:23:59'))
                    ]"
                />
                <filter
                    name="effective_yesterday"
                    string="Yesterday"
                    domain="[
                    ('effective_date', '&gt;=', (datetime.datetime.now()-datetime.timedelta(days=1)).strftime('%Y-%m-%d 00:00:00')),
                    ('effective_date', '&lt;=',(datetime.datetime.now()-datetime.timedelta(days=1)).strftime('%Y-%m-%d 23:23:59'))
                    ]"
                />
                <filter name="effective_date" string="Period" date="effective_date" />
                <separator />
                <filter name="inactive" string="Archived" domain="[('active', '=', False)]" />
                <group string="Group By">
                    <filter name="Patient" context="{'group_by': 'patient_id'}" />
                    <filter name="Encounter " context="{'group_by': 'encounter_id'}" />
                </group>
            </search>
        </field>
    </record>
    <record id="ob_sheet_view_tree" model="ir.ui.view">
        <field name="name">ob.sheet.view.tree</field>
        <field name="model">ni.observation.sheet</field>
        <field name="arch" type="xml">
            <tree>
                <field name="patient_id" />
                <field name="encounter_id" />
                <field name="category_ids" widget="many2many_tags" options="{'color_field': 'color'}" optional="show" />
                <field name="performer_ref" optional="hide" />
                <field name="observation_ids" />
                <field name="note" optional="hide" />
                <field name="active" optional="hide" />
                <field name="effective_date" />
                <field name="company_id" groups="base.group_multi_company" optional="show" />
            </tree>
        </field>
    </record>
    <record id="ob_sheet_view_graph" model="ir.ui.view">
        <field name="name">ob.line.view.graph</field>
        <field name="model">ni.observation.sheet</field>
        <field name="arch" type="xml">
            <graph stacked="False" type="bar">
                <field name="patient_id" type="col" />
                <field name="effective_date" interval="day" />
            </graph>
        </field>
    </record>
    <record id="ob_sheet_view_form" model="ir.ui.view">
        <field name="name">ob.sheet.view.form</field>
        <field name="model">ni.observation.sheet</field>
        <field name="arch" type="xml">
            <form>
                <field name="id" invisible="True" />
                <field name="active" invisible="True" />
                <sheet>
                    <div name="button_box" class="oe_button_box" groups="base.group_user">
                        <button
                            name="action_patient_observation_graph"
                            string="Graph"
                            class="oe_stat_button"
                            icon="fa-bar-chart"
                            inivisible="[('id','=', False)]"
                            type="object"
                        />
                    </div>
                    <widget
                        name="web_ribbon"
                        attrs="{'invisible': [('active', '=', True)]}"
                        bg_color="bg-danger"
                        title="Archived"
                    />
                    <group>
                        <group>
                            <field name="patient_id" />
                            <field name="encounter_id" context="{'show_state': True}" />
                            <label for="category_ids" />
                            <div>
                                <field
                                    name="category_ids"
                                    options="{'color_field': 'color', 'no_open':True, 'no_create':True}"
                                    widget="many2many_tags"
                                    attrs="{'readonly': [('patient_id', '=', False)]}"
                                />
                                <button
                                    class="oe_edit_only btn-primary"
                                    string="Apply"
                                    name="action_line_by_category_ids"
                                    type="object"
                                    attrs="{'invisible': ['|', ('category_ids', '=', False), ('patient_id', '=', False)]}"
                                />
                            </div>
                        </group>
                        <group>
                            <field name="company_id" groups="base.group_multi_company" />
                            <field name="effective_date" />
                            <field name="performer_ref" invisible="1" />
                        </group>
                    </group>
                    <notebook attrs="{'invisible': [('id', '=', False)]}">
                        <page string="Detail">
                            <field
                                name="observation_ids"
                                context="{'default_sheet_id':id, 'default_patient_id': patient_id, 'default_encounter_id': encounter_id, 'default_effective_date': effective_date}"
                            >
                                <tree
                                    decoration-danger="display_class == 'danger'"
                                    decoration-info="display_class == 'info'"
                                    decoration-muted="display_class == 'muted'"
                                    decoration-primary="display_class == 'primary'"
                                    decoration-success="display_class == 'success'"
                                    decoration-warning="display_class == 'warning'"
                                    editable="bottom"
                                    default_order="sequence"
                                >
                                    <field name="patient_id" invisible="1" />
                                    <field name="encounter_id" invisible="1" />
                                    <field name="event_id" invisible="1" />
                                    <field name="effective_date" invisible="1" />
                                    <field name="sequence" widget="handle" />
                                    <field name="category_id" optional="hide" />
                                    <field name="type_id" />
                                    <field name="value" />
                                    <field name="unit" />
                                    <field name="interpretation_id" />
                                    <field name="display_class" invisible="True" />
                                    <button name="view_graph" string="View Graph" icon="fa-bar-chart" type="object" />
                                </tree>
                            </field>
                        </page>
                        <page string="Note">
                            <field name="note" placeholder="Additional Note..." />
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>
</odoo>
